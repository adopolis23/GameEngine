#pragma once

// Fix these include paths later
#include "application.h"

// Contains the main entry point and expects the client to define the function Application* CreateApplication();
#include "EntryPoint.h"
